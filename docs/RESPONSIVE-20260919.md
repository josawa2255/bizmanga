# レスポンシブ修正・検証記録（2026-09-19 / #37）

スマートフォン・タブレット・PCの調査で確認した14件を修正。再検査で見つかった、低い横長画面でのビズアニメ冒頭の欠けと、制作事例の件数付きフィルターのはみ出しも修正した。

## 変更と確認の対応

| 調査項目 | 修正 | 確認方法 |
|---|---|---|
| 1〜2 ヘッダーの衝突・配置差 | ナビのみ1024px以下をドロワー化。共通フォント・flexを統一し、初期CSSの境界・高さ・余白も同期 | 全件寸法検査、料金・漫画家・会社紹介・強み・ビズアニメの1440px位置比較 |
| 3 タッチの親メニュー | 初回で子項目を開き、2回目で親ページへ移動 | Chromium/WebKit、820/1180pxのタップ。PCキーボードとEscapeも検証 |
| 4 回転後のロック | 境界を跨ぐ際にメニュー・子メニュー・ARIA・bodyロックを解除 | ナビ回帰テスト、820→1180px |
| 5 ホーム3画面 | 1024px以下は縦1列。PC幅と間隔を縮小可能にし、GSAPも同じ境界で再設定 | 1180→820→1024→1025→1180px、HTTPSオリジンのローカル応答差し替えで埋め込み画像読み込み |
| 6〜7 漫画家固定CTA | 48px操作領域、展開時縦積み、安全領域、詳細モーダル中非表示 | 390pxで開閉・復帰・モーダル最下部 |
| 8 漫画家の冒頭 | 固定heightを内容に追従するmin-heightへ変更 | 1920×600pxでCTAがヒーロー内に収まることを検査 |
| 9〜11 本文折り返し | Grid最小幅とFlex子の縮小、日本語auto-phrase未対応時の折り返し | 全件検査、特にWebKitのホーム・FAQ・活用場面 |
| 12 コラム表 | 共通JSで表だけをラップ、横スクロール案内・キーボード操作を追加 | 静的記事と、サニタイズ後に挿入される動的記事の12列表フィクスチャ |
| 13 料金CTA | 最大幅を親に合わせ、複数行を許可 | 320pxの全件検査 |
| 14 漫画見開き | 100pxの余白をサイドバーがある縦読みだけに限定 | 見開き→縦読み→768px回転、QR直リンク・最終CTA |
| 追加: ビズアニメ横長 | 幅1024px以下かつ高さ600px以下は自然な縦スクロールにし、演出進捗をリセット | 667×375px・640×400px |
| 追加: 制作事例フィルター | 3列を縮小可能にし、件数付きラベルを折り返す | WPデータ読込後の320px |

API・送信先・計測イベントの変更なし。HTML/テンプレートの変更は初期表示用ナビCSSと表ヘルパー読み込みに限定され、本文・リンク・作品IDを変更していないことを差分比較した。新しい表ヘルパーはDOMを移動するだけで、HTML文字列を生成・挿入せず、既存のサニタイズ後の本文に適用される。

## 自動検査

結果集計は `docs/responsive-20260919/` に保存する。全件監査の大きな生データ・スクリーンショットはローカルの `%TEMP%/bizmanga-responsive-fixed/` に保存。

| 検査 | 結果 | 記録 |
|---|---|---|
| 全ページの寸法 | 2,222条件、検出対象の不具合0件（修正後の再実行を含む） | [ページ別集計](responsive-20260919/layout-summary.json) |
| 漫画・WPスモーク | 48 PASS / 0 FAIL | [全項目ログ](responsive-20260919/smoke-results.txt) |
| ナビ回帰 | 既存48＋タブレット8＝56 PASS / 0 FAIL | `tools/test_mobile_nav.py` |
| 境界・フォーム・モーダル・表・回転等 | 重複ラベルを除く130項目 PASS | [項目別結果](responsive-20260919/interactive-results.json) |
| 事例モーダル・WP障害・HTTPS埋め込み等 | 30 PASS / 0 FAIL | [項目別結果](responsive-20260919/integration-results.json) |

目視用: [タブレットメニュー](responsive-20260919/tablet-menu.png)、[漫画家CTA](responsive-20260919/artist-cta.png)、[作家モーダル最下部](responsive-20260919/artist-modal.png)、[ホームPC](responsive-20260919/home-pc.png)。

- 寸法検査: 101 HTML。Chromium 18サイズ、WebKit 4サイズ、計2,222条件。転送ページは転送完了後の表示を検査。
- 追加操作: 境界前後（767/768/769、899/900/901、1023/1024/1025、1099/1100/1101）、1920×600、回転、640×400でのリフロー。
- 640×400は1280×800の200%ズームに相当するCSS表示領域の検査。実ブラウザーUIのズーム操作や実機Safari確認の代替とはしない。
- フォームは非GETリクエストを遮断・模擬応答し、必須入力、送信中、成功、失敗後の復帰を検査。実送信なし。
- WP障害時のホーム・事例・漫画家、長文・画像付きのお客様の声フィクスチャ、漫画家の絞り込み・空状態・解除を検査。
- 各生成処理は一時コピー内で実行し、4件とも終了コード0。漫画家11件、コラム39ファイル（転送を含む）と表ヘルパー保持を確認。取得時点の本文・日時・データの差分は本ブランチに取り込まない。
- 変更JSの構文確認、Pythonテストのコンパイル、`git diff --check`を実施。

初回実行で検出した読み込みタイミングの失敗は放置せず、対象要素の初期化・遅延画像・表示アニメーションの完了を待って再実行する。レイアウト不具合はソースを直し、該当ページの全サイズを再検査する。

途中で監査内蔵サーバーが終了し、既存のVS Code Live Server（同じ5500番）に切り替わったため、拡張子なしURLで404とコラム転送ループが発生した。VS Codeプロセスは終了せず、127.0.0.1限定のCleanURLサーバーを起動してURLの200応答を確認後、影響項目を再実行した。合格数にはこの誤ったサーバーでの実行を含めない。

### 再実行

```powershell
python tools/test_responsive_layout.py --serve --output "$env:TEMP/bizmanga-responsive-audit"
```

操作テストは拡張子なしURLを扱える `serve.py` のハンドラーで5500番ポートを使う。ポート5500は既存WP CORSの許可対象。

```powershell
python tools/test_mobile_nav.py --base http://127.0.0.1:5500
python tools/test_responsive_states.py --output "$env:TEMP/bizmanga-responsive-states"
python tools/test_responsive_integrations.py --output "$env:TEMP/bizmanga-responsive-integrations"
python tools/smoke-manga-wp.py --base http://127.0.0.1:5500
```

独立した漫画/WP検証は既存手順どおり `python tools/smoke-manga-wp.py --serve .` で起動と検証をまとめて行える。既に5500番が起動中なら上記`--base`を使う。

## 公開前に残す確認

- 実機iPad Safari: 縦横、Split View、ブラウザーUI伸縮。
- 実機スマートフォン: 安全領域、キーボード表示時、実ブラウザー200%ズーム。
- [漫画/WPレビュー手順](REVIEW-MANGA-WP.md) §5の `/code-review high`・`/security-review` と平澤さんの承認。

本記録の自動検査・差分確認は上記の独立レビューの完了を意味しない。承認後にマージし、公開後の主要画面・漫画/WPスモークを実施する。重大な不具合時はPR単位で戻す。
